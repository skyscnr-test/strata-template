clone:
  clone:
    image: 325714046698.dkr.ecr.eu-west-1.amazonaws.com/dmx/drone-git:next
    pull: true

pipeline:
  replicate-code:
    image: gliderlabs/alpine:3.6
    commands:
      - sh replicate.sh
    secrets:
      # Contact Cassini about these secrets
      - source: codecommit_user
        target: CODECOMMIT_USER
      - source: codecommit_pw
        target: CODECOMMIT_PW
    when:
      branch: master
      event:
        exclude: pull_request